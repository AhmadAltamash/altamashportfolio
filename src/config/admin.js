// The ONLY Google account allowed into /admin.
// This is a client-side convenience check for the UI — the real
// enforcement happens in your Firestore Security Rules (see
// ADMIN_SETUP.md), which is what actually stops anyone else from
// writing data even if they inspect this file.
export const ADMIN_EMAIL = "altamashahmad910@gmail.com";
